---
# ---------------------------------------------------------------------------
# onetime / vars / main.yml
# ---------------------------------------------------------------------------

# Marker file for this operation (assembles from operation_id)
# Actual path set in tasks via set_fact.

# Audit-log file for this run
# onetime_audit_log_file = onetime_audit_log_dir / onetime_operation_id.log

# Templates
onetime_summary_template: onetime_summary.txt.j2
onetime_preflight_report: preflight_report.txt.j2

# Static helper script shipped via files/
onetime_wrapper_script: onetime_wrapper.sh
