---
# ---------------------------------------------------------------------------
# onetime / meta / main.yml
# ---------------------------------------------------------------------------
galaxy_info:
  author: "platform-engineering"
  description: >
    One-time migration / setup role.  Idempotency is guaranteed via marker
    files so a re-run is a no-op unless forced.  Supports pre-flight checks,
    a configurable rollback script on failure, and full audit logging.
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["20.04", "22.04", "24.04"]
    - name: CentOS
      versions: ["8", "9"]
    - name: RedHat
      versions: ["8", "9"]

dependencies: []
