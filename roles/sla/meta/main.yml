---
# ---------------------------------------------------------------------------
# sla / meta / main.yml
# ---------------------------------------------------------------------------
galaxy_info:
  author: "platform-engineering"
  description: >
    SLA-monitoring role.  Evaluates uptime, response-time, and error-rate
    SLAs using a custom sla_check module.  Persists breach state to disk
    and fires Slack / PagerDuty notifications on any violation.
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["20.04", "22.04", "24.04"]
    - name: CentOS
      versions: ["8", "9"]
    - name: RedHat
      versions: ["8", "9"]

dependencies: []
