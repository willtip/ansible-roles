---
# ---------------------------------------------------------------------------
# configuration / meta / main.yml
# ---------------------------------------------------------------------------
galaxy_info:
  author: "platform-engineering"
  description: >
    Idempotent system-configuration role.  Manages NTP (chrony), kernel
    sysctl tuning, rsyslog (with optional remote forwarding), and the
    login banner (MOTD).  All changes are backed up before overwrite.
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["20.04", "22.04", "24.04"]
    - name: CentOS
      versions: ["8", "9"]
    - name: RedHat
      versions: ["8", "9"]

dependencies: []
