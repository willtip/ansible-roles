================================================================================
  APPROVAL REQUEST
  Gate           : {{ approvals_gate_name }}
  Host           : {{ inventory_hostname }}
  Requested at   : {{ ansible_date_time.iso8601 }}
  Timeout        : {{ approvals_timeout_seconds }} seconds
================================================================================

An Ansible playbook requires manual approval before continuing.

To APPROVE:
    echo "approved" > {{ approvals_state_dir }}/{{ approvals_gate_name }}_decision.json

To REJECT:
    echo "rejected" > {{ approvals_state_dir }}/{{ approvals_gate_name }}_decision.json

{% if approvals_escalation_enabled %}
If no decision is made within {{ approvals_escalation_timeout_seconds }} seconds,
an escalation notification will be sent to:
{% for contact in approvals_escalation_contacts %}
  - {{ contact }}
{% endfor %}
{% endif %}

================================================================================
