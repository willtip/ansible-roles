---
# ---------------------------------------------------------------------------
# approvals / meta / main.yml
# ---------------------------------------------------------------------------
galaxy_info:
  author: "platform-engineering"
  description: >
    Approval-gate role.  Pauses a playbook at a named gate and waits for a
    human decision (approved / rejected).  Supports configurable timeouts,
    escalation notifications, and Slack integration.
  license: "MIT"
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: ["20.04", "22.04", "24.04"]
    - name: CentOS
      versions: ["8", "9"]
    - name: RedHat
      versions: ["8", "9"]

dependencies: []
